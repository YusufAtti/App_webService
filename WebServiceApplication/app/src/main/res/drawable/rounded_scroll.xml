<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_blue" /> <!-- Butonun arka plan rengi -->
    <corners android:radius="16dp" /> <!-- 16dp kavis ekleniyor -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
    <stroke
        android:width="2dp"
        android:color="@color/dark_blue" /> <!-- Butonun kenar çizgisi -->
</shape>
